# Where the storefront data lives. Point this at a WooCommerce base URL to
# swap the bundled demo catalogue for a live shop.
STORE_SOURCE=demo

# Bind address for the Streamable HTTP endpoint
HOST=127.0.0.1
PORT=8080
